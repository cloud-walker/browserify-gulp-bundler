{
  "scripts": {
    "start": "gulp",
    "test": "gulp test",
    "nomatch": "gulp clean nomatch",
    "single": "gulp clean single",
    "multi": "gulp clean multi"
  },
  "devDependencies": {
    "del": "^2.1.0",
    "gulp": "^3.9.0",
    "gulp-sequence": "^0.4.1",
    "lazypipe": "^1.0.1"
  }
}
